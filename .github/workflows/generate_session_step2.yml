name: "Generate Session — Step 2: Complete Auth"

on:
  workflow_dispatch:
    inputs:
      auth_token:
        description: 'AUTH_TOKEN из логов Step 1 (одна длинная строка)'
        required: true
      code:
        description: 'Код из Telegram (5-6 цифр)'
        required: true
      phone:
        description: 'Тот же номер телефона что в Step 1'
        required: true
      api_id:
        description: 'TELEGRAM_API_ID'
        required: true
      api_hash:
        description: 'TELEGRAM_API_HASH'
        required: true
      password:
        description: 'Пароль 2FA (если включён, иначе оставь пустым)'
        required: false
        default: ''

jobs:
  complete_auth:
    runs-on: ubuntu-latest
    steps:
      - name: Install telethon
        run: pip install telethon

      - name: Complete authentication and print session string
        env:
          AUTH_TOKEN: ${{ github.event.inputs.auth_token }}
          CODE:       ${{ github.event.inputs.code }}
          PHONE:      ${{ github.event.inputs.phone }}
          API_ID:     ${{ github.event.inputs.api_id }}
          API_HASH:   ${{ github.event.inputs.api_hash }}
          PASSWORD:   ${{ github.event.inputs.password }}
        run: |
          python3 - << 'EOF'
          import asyncio, os, json, base64
          from telethon import TelegramClient
          from telethon.sessions import StringSession
          from telethon.errors import SessionPasswordNeededError

          async def main():
              token = os.environ["AUTH_TOKEN"].strip()
              data = json.loads(base64.b64decode(token).decode())
              partial_session = data["s"]
              phone_code_hash = data["h"]

              client = TelegramClient(
                  StringSession(partial_session),
                  int(os.environ["API_ID"]),
                  os.environ["API_HASH"],
              )
              await client.connect()
              try:
                  await client.sign_in(
                      os.environ["PHONE"],
                      os.environ["CODE"].strip(),
                      phone_code_hash=phone_code_hash,
                  )
              except SessionPasswordNeededError:
                  pw = os.environ.get("PASSWORD", "").strip()
                  if not pw:
                      raise RuntimeError("2FA включён — введи пароль в поле 'password'")
                  await client.sign_in(password=pw)

              me = await client.get_me()
              session = client.session.save()
              await client.disconnect()

              print(f"Вошёл как: {me.first_name} (@{getattr(me, 'username', 'нет')})")
              print()
              print("=" * 60)
              print("TELEGRAM_SESSION_STR =", session)
              print("=" * 60)
              print()
              print("Добавь в GitHub Secrets:")
              print("Settings → Secrets and variables → Actions → New repository secret")
              print("Name: TELEGRAM_SESSION_STR")

          asyncio.run(main())
          EOF
